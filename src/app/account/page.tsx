import { requireUser } from "@/modules/auth/session";
import { db } from "@/lib/db";
import { formatDate, formatMoney } from "@/lib/format";
import type { Order } from "@generated/prisma/client";
export default async function AccountPage(){const user=await requireUser();const orders=await db.order.findMany({where:{userId:user.id},orderBy:{createdAt:"desc"},take:20});return <main className="section container"><div className="panel-head"><div><span className="eyebrow">حساب مشتری</span><h1>{user.firstName ? `${user.firstName} ${user.lastName??""}` : user.email}</h1></div><form action="/api/auth/logout" method="post"><button className="btn">خروج</button></form></div><div className="stats"><div className="stat"><strong>{orders.length.toLocaleString("fa-IR")}</strong><span>سفارش ثبت‌شده</span></div><div className="stat"><strong>{orders.filter((o: Order)=>o.status==="DELIVERED").length.toLocaleString("fa-IR")}</strong><span>تحویل‌شده</span></div><div className="stat"><strong>{user.phone??"—"}</strong><span>شماره تماس</span></div><div className="stat"><strong style={{fontSize:"1rem"}}>{user.email}</strong><span>ایمیل حساب</span></div></div><div className="section-head" style={{marginTop:45}}><div><h2>سفارش‌های من</h2></div></div><div className="table-wrap"><table><thead><tr><th>شماره سفارش</th><th>مبلغ</th><th>وضعیت</th><th>تاریخ</th></tr></thead><tbody>{orders.map((o: Order)=><tr key={o.id}><td>{o.orderNumber}</td><td>{formatMoney(o.total.toString())}</td><td><span className="badge">{o.status}</span></td><td>{formatDate(o.createdAt)}</td></tr>)}</tbody></table>{!orders.length&&<div className="empty">هنوز سفارشی ندارید.</div>}</div></main>}
