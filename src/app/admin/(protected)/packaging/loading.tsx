import { BpPanelListSkeleton } from "@/components/admin/blueprint/skeleton";

export default function PackagingLoading() {
  return <BpPanelListSkeleton columns={9} rows={5} minWidth={880} leading={1} rowHeight={57} />;
}
