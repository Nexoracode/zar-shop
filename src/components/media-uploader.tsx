"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function MediaUploader(){const router=useRouter();const[msg,setMsg]=useState("");const[loading,setLoading]=useState(false);async function upload(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setMsg("");const response=await fetch("/api/media",{method:"POST",body:new FormData(e.currentTarget)});const result=await response.json();setLoading(false);if(!response.ok){setMsg(result.message??"بارگذاری ناموفق بود.");return}setMsg("فایل با موفقیت به گالری اضافه شد.");e.currentTarget.reset();router.refresh()}return <form className="card card-body form" onSubmit={upload}><div className="form-row"><div className="field"><label htmlFor="mediaFile">تصویر یا ویدیو</label><input id="mediaFile" name="file" type="file" accept="image/jpeg,image/png,image/webp,video/mp4,video/webm" required/></div><div className="field"><label htmlFor="mediaTitle">عنوان</label><input id="mediaTitle" name="title"/></div></div>{msg&&<div className={msg.includes("موفقیت")?"success":"error"}>{msg}</div>}<button className="btn btn-primary" disabled={loading}>{loading?"در حال بارگذاری...":"افزودن به گالری"}</button></form>}
