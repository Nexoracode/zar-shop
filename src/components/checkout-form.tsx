"use client";
import { useState } from "react";
export function CheckoutForm(){const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/checkout",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const data=await r.json();if(!r.ok){setError(data.message??"ثبت سفارش ناموفق بود.");setLoading(false);return}window.location.assign(data.redirectUrl)}return <form className="form card card-body" onSubmit={submit}><h2 style={{margin:0}}>اطلاعات تحویل و فاکتور</h2><div className="form-row"><div className="field"><label htmlFor="recipient">نام تحویل‌گیرنده</label><input id="recipient" name="recipient" required/></div><div className="field"><label htmlFor="checkoutPhone">شماره تماس</label><input id="checkoutPhone" name="phone" dir="ltr" pattern="09[0-9]{9}" required/></div></div><div className="form-row"><div className="field"><label htmlFor="province">استان</label><input id="province" name="province" required/></div><div className="field"><label htmlFor="city">شهر</label><input id="city" name="city" required/></div></div><div className="field"><label htmlFor="postalCode">کد پستی</label><input id="postalCode" name="postalCode" dir="ltr" required/></div><div className="field"><label htmlFor="addressLine">نشانی کامل</label><textarea id="addressLine" name="addressLine" rows={3} required/></div>{error&&<div className="error">{error}</div>}<button className="btn btn-gold" disabled={loading}>{loading?"در حال ایجاد سفارش...":"ثبت سفارش و پرداخت"}</button></form>}
