"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function ProductForm(){const router=useRouter();const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const f=new FormData(e.currentTarget);const body={sku:f.get("sku"),name:f.get("name"),slug:f.get("slug"),description:f.get("description"),purity:Number(f.get("purity")),weightGrams:Number(f.get("weightGrams")),makingFeeType:f.get("makingFeeType"),makingFeeValue:Number(f.get("makingFeeValue")),profitPercent:Number(f.get("profitPercent")),taxPercent:Number(f.get("taxPercent")),stock:Number(f.get("stock")),status:f.get("status"),featured:f.get("featured")==="on"};const r=await fetch("/api/products",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const result=await r.json();if(!r.ok){setError(result.message??"ثبت ناموفق بود.");setLoading(false);return}router.push("/admin/products");router.refresh()}return <form className="form card-body card" onSubmit={submit}><div className="form-row"><div className="field"><label>نام محصول</label><input name="name" required/></div><div className="field"><label>کد کالا</label><input name="sku" dir="ltr" required/></div></div><div className="field"><label>نشانی انگلیسی</label><input name="slug" dir="ltr" pattern="[a-z0-9-]+" required/></div><div className="field"><label>توضیحات</label><textarea name="description" rows={4}/></div><div className="form-row"><div className="field"><label>وزن (گرم)</label><input name="weightGrams" type="number" step="0.001" min="0.001" required/></div><div className="field"><label>عیار</label><input name="purity" type="number" defaultValue="750" required/></div></div><div className="form-row"><div className="field"><label>نوع اجرت</label><select name="makingFeeType"><option value="PERCENT">درصدی</option><option value="FIXED">مبلغ ثابت</option></select></div><div className="field"><label>مقدار اجرت</label><input name="makingFeeValue" type="number" defaultValue="10" min="0" required/></div></div><div className="form-row"><div className="field"><label>درصد سود</label><input name="profitPercent" type="number" defaultValue="7" min="0" required/></div><div className="field"><label>درصد مالیات</label><input name="taxPercent" type="number" defaultValue="10" min="0" required/></div></div><div className="form-row"><div className="field"><label>موجودی</label><input name="stock" type="number" defaultValue="1" min="0" required/></div><div className="field"><label>وضعیت</label><select name="status"><option value="DRAFT">پیش‌نویس</option><option value="ACTIVE">منتشرشده</option><option value="ARCHIVED">بایگانی</option></select></div></div><label><input name="featured" type="checkbox"/> نمایش در محصولات ویژه</label>{error&&<div className="error">{error}</div>}<button className="btn btn-primary" disabled={loading}>{loading?"در حال ثبت...":"ثبت محصول"}</button></form>}
