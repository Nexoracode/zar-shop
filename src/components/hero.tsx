"use client";

import { Alert, Button, Card, Chip, Input, Table } from "@heroui/react";

export { Button, Card, Input, Table };
export const AlertRoot = Alert;
export const AlertDescription = Alert.Description;
export const CardContent = Card.Content;
export const ChipRoot = Chip;
export const ChipLabel = Chip.Label;
export const TableScrollContainer = Table.ScrollContainer;
export const TableContent = Table.Content;
export const TableHeader = Table.Header;
export const TableColumn = Table.Column;
export const TableBody = Table.Body;
export const TableRow = Table.Row;
export const TableCell = Table.Cell;
